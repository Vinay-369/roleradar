version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: roleradar-mongo
    ports:
      - "27017:27017"
    volumes:
      - roleradar_mongo_data:/data/db
    restart: unless-stopped

volumes:
  roleradar_mongo_data:
